# superTypora · 全景规划文档（Master Plan）

> 这是我们的**唯一对齐锚点**。任何时候迷失了、忘了在做什么，回到这份文档。
> **滚动式更新**：每完成并验证一个模块，就更新「§7 当前进度」和「§5 全景蓝图」的状态标记。

---

## 一、目标（愿景）

做一个**真正所见即所得的 Markdown 笔记软件**——像 Typora 一样：单栏、边打边渲染、光标处显源码、格式无损；左侧有大纲导航；能在「所见即所得」和「纯源码」之间随时切换。本地优先，桌面（Electron）+ 网页双端。

**一句话**：*Typora 式的手感 + 笔记软件的完整度 + 格式无损的信任感。*

---

## 二、宪法（铁律 · 不可违背）

这些是我们用踩过的坑换来的，违背任何一条都会翻车：

1. **格式无损是架构红线。** 源码（markdown 原文）永远是唯一真相，渲染只是叠加在文本上的"装饰层"。绝不用"文档模型 ↔ markdown 来回序列化"的架构（那必然改写 `*`→`-`、重排表格）。→ 这就是我们选 CodeMirror 6、否掉 Crepe/TipTap/Vditor 的根本原因。
2. **核心是"所见即所得的流畅手感"，不是功能堆料。** 渲染做得出不算赢，打字卡、列表不自动续、中文吞字就是废。**中文输入法丝滑 = 生死线。**
3. **一步一模块，实现一个验证一个。** 专注当下模块，做完立即验证（编译 + 实机），验证过了再前进。不一次铺开。
4. **诚实执行，只认真实验证。** 只相信真实的命令返回 / 实机效果，**绝不编造"成功""已完成""测试通过"**。声称任何结果前，必须有真实证据。（这是主控此前犯过的错，制度化杜绝。）
5. **保留回退，不破坏可用版本。** 每个模块推进时保留上一个可用状态，随时能退回。旧编辑器（Crepe）文件暂不删除。
6. **不忘全景。** 专注当下模块的同时，每一步都要问："这一步服务于所见即所得这个核心吗？"偏离核心的花哨功能一律后置。
7. **改动可控、说到做到。** 改源码前说清楚改哪里；工期诚实估算，不乐观虚报。

---

## 三、需求（已锁定）

### 三大核心
1. **所见即所得**：**所有元素**就地渲染（标题、粗斜体、删除线、行内码、高亮、列表、任务列表、引用、代码块、表格、图片、链接、分割线、数学公式、mermaid）。
2. **源码 ⇄ 所见即所得** 两视图随时切换。
3. **大纲侧栏**，可折叠 / 收起，点标题跳转。

### 第一版（MVP）功能全集
- **就地渲染**：上述全部元素
- **输入手感**：中文 IME 丝滑、Markdown 自动行为（列表续写 / 有序自增 / 表格 Tab / `#` 变标题）、快捷键（⌘B/I/K）、撤销重做
- **导航 / 信息**：大纲侧栏、字数统计
- **输入输出**：图片粘贴自动落地、md/html 导出（已有）、**导出 PDF**
- **效率**：**斜杠命令 `/`**、**查找/替换 ⌘F**、**从 Word/网页粘贴自动转 markdown**
- **已有保留**：自动保存、文件抽屉、拖拽导入

### 后置（第一版之后再议）
双链 `[[]]`、标签、专注 / 打字机模式、多主题、大纲拖拽调整章节。

---

## 四、技术选型（一次性决策，已定）

**方案：CodeMirror 6 + Decoration（Obsidian Live Preview 架构）自研。**

- 经两轮共 10 个独立视角调研 + 红队对抗验证收敛得出。
- **为什么是它**：唯一能同时满足「格式 byte 级无损 + Typora 式就地渲染 + 源码/预览同一份数据零成本切换」的架构。全球验证：Obsidian、Logseq 的 Live Preview 都是这套。
- **排除**：Crepe / TipTap / Milkdown（ProseMirror 富文本，序列化必然改写格式）；Vditor（Lute 引擎会规范化，Issue #464 吞空行）；Cherry / ByteMD（分屏预览，非就地渲染）。
- **已知硬骨头 & 对策**：
  - **表格就地可视化编辑**是无底洞（连 Obsidian 都降级）→ 先"渲染成表格 + 光标进入显源码"，可视化编辑后置。
  - **富块（代码/表格/图/公式）的 widget 用纯 DOM 渲染，不塞 React**（CM6 作者明确反对）。
  - **中文 IME × 装饰**是高危区 → 每个涉及光标/装饰的模块都要实机测中文输入。

---

## 五、全景蓝图（完整功能地图 · 带状态）

状态图例：✅ 完成并验证 ｜ �buil: 已实现待验证 ｜ ⬜ 未开始

### A. 就地渲染引擎
| 元素 | 状态 |
|---|---|
| 标题 H1–H6 | 🚧 待实机验证 |
| 粗体 / 斜体 / 删除线 / 行内代码 | 🚧 待实机验证 |
| 高亮 `==` | ⬜ |
| 无序 / 有序列表 | ⬜ |
| 任务列表（可勾选 ☑） | ⬜ |
| 引用块 | ⬜ |
| 分割线 | ⬜ |
| 代码块（语法高亮框） | ⬜ |
| 表格（渲染，编辑降级源码） | ⬜ |
| 图片（就地显示） | ⬜ |
| 链接 | ⬜ |
| 数学公式（KaTeX） | ⬜ |
| mermaid 图 | ⬜ |

### B. 输入手感
| 项 | 状态 |
|---|---|
| 光标处显源码 / 离开渲染 | 🚧 待验证 |
| 中文 IME 丝滑 | 🚧 **待实机验证（生死线）** |
| 列表回车自动续写 | ⬜ |
| 有序列表自增 / 表格 Tab 跳格 | ⬜ |
| 快捷键 ⌘B/I/K | ⬜ |
| 撤销 / 重做 | 🚧（CM6 自带，待验证） |

### C. 视图 / 导航
| 项 | 状态 |
|---|---|
| 源码 ⇄ 所见即所得 切换 | ⬜（引擎已支持 Compartment，待接 UI） |
| 大纲侧栏（折叠 / 跳转） | ⬜ |
| 字数统计 | ⬜ |

### D. 输入输出 / 效率
| 项 | 状态 |
|---|---|
| 图片粘贴自动落地 | ⬜ |
| 导出 md / html | ✅（项目已有） |
| 导出 PDF | ⬜ |
| 斜杠命令 `/` | ⬜ |
| 查找 / 替换 ⌘F | ⬜ |
| 粘贴富文本 → markdown | ⬜ |

### E. 已有基础设施（复用）
自动保存 ✅ ｜ 文件抽屉 ✅ ｜ 拖拽导入 ✅ ｜ 桌面打包 ✅

---

## 六、执行路线（滚动式，一阶段 = 一个可验证模块群）

> 顺序可按反馈微调，但每一阶段结束都必须过「§8 验收闸门」再进下一阶段。

- **M0 · 地基 + 行内渲染**：CM6 接入、替换旧编辑器、源码即 doc（无损地基）、行内元素（标题/粗斜/删除线/行内码）就地渲染 + 光标显隐 + IME 基础处理。→ *已实现，正在实机验证*
- **M1 · 源码切换**：工具栏按钮 + 快捷键，在所见即所得 / 纯源码间切换（引擎已支持，接 UI）。*先做，因为它是核心且简单，早点让你随时能看源码。*
- **M2 · 块级基础元素**：列表（无序/有序/任务勾选）、引用块、分割线、高亮 `==`。
- **M3 · 富块 widget**：代码块（高亮框）、图片、mermaid、数学公式、表格（渲染 + 编辑降级源码）。纯 DOM widget，复用现有渲染。
- **M4 · 大纲侧栏**：标题层级、点击跳转、折叠收起。
- **M5 · 输入手感增强**：列表自动续写、有序自增、表格 Tab、快捷键 ⌘B/I/K。
- **M6 · 笔记功能**：图片粘贴落地、字数统计。
- **M7 · 效率增强**：斜杠命令 `/`、查找替换、粘贴转 markdown、导出 PDF。

每阶段末：编译通过 + 实机验证 + 打包一版让你确认 → 更新本文档 → 进下一阶段。

---

## 七、当前进度（每次滚动更新这里）

- **当前阶段：M0（地基 + 行内渲染）**
- **已完成**：
  - CodeMirror 6 Live Preview 编辑器（`components/editor/live-preview-editor.tsx`）
  - 已接入主编辑器（`editor-shell.tsx` 用 LivePreviewEditor 替换 Crepe），Crepe 文件保留作回退
  - 行内就地渲染：标题、粗体、斜体、删除线、行内代码 + 光标显隐 + IME 组合期跳过重算
  - tsc 0 错误、build 通过、已打包桌面 app（18:35 版）
- **正在做**：**你实机验证 M0 手感** —— 重点：中文输入是否丝滑、光标显隐是否正确、格式是否无损
- **下一步（待你验证 M0 通过后）**：进入 **M1 源码切换**
- **最近更新**：2026-07-02

---

## 八、验收闸门（每个模块过关标准）

任意一条不过，不算完成、不进下一阶段：

1. **格式无损**：打开一份外部 `.md`，改一个字保存，`git diff` **只有那一行**。
2. **中文输入**：连续打中文 200 字，不卡、不吞字、不跳光标。
3. **就地渲染正确**：目标元素光标移出即渲染、移入显源码可编辑。
4. **不崩不闪**：光标稳定、无渲染闪烁、无内容跳动。
5. **源码切换**（相关阶段）：切换时守住光标 / 滚动位置。
6. **编译通过**：tsc 0 错误 + build 成功。

---

## 九、风险与约定

- **表格**是最大风险，明确降级（渲染 + 编辑显源码），不在 MVP 追求单元格可视化编辑。
- **中文 IME** 每个装饰相关模块都要实机测，不能只看编译过。
- **每阶段保留回退**：旧 Crepe 编辑器文件不删，直到新编辑器全面稳定。
- **主控诚实约束**：任何"完成/通过"的声明必须有真实命令返回或实机证据支撑（见宪法 §2.4）。
- 本文档随进度滚动更新，是唯一权威对齐源。
