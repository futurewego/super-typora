# SuperTypora 桌面端效果演示

欢迎使用 **SuperTypora** —— 一个本地优先的 Markdown 工作台。这个文件用来快速展示编辑区与预览区的渲染效果。

## 文本样式

支持 **粗体**、*斜体*、~~删除线~~、`行内代码`，以及 [超链接](https://example.com)。

> 引用块：时间 > 代码洁癖，验证 > 完美架构。
> —— 一句被反复验证的工程信条。

## 列表

无序列表：

* 极简双栏：编辑 ｜ 预览

* 文件抽屉：`⌘\` 呼出

* macOS 原生毛玻璃 vibrancy

有序列表：

1. 启动直达工作区（无营销首页）
2. 拖拽 `.md` 文件即可导入
3. `⌘S` 存回磁盘

任务列表：

* [x] vibrancy 毛玻璃

* [x] 文件抽屉

* [ ] 你正在看的这一刻

## 代码块

```ts
function greet(name: string): string {
  return `Hello, ${name} — from SuperTypora`;
}

console.log(greet("Marvin"));
```

## 表格

| 模块  | 状态 | 说明                      |
| --- | -- | ----------------------- |
| 工具栏 | ✅  | 紧凑桌面风，去 pill 大写         |
| 主题  | ✅  | 编辑器跟随亮暗切换               |
| 强调色 | ✅  | 跟随 macOS 系统 AccentColor |

## 流程图（Mermaid）

```mermaid
flowchart LR
  A[启动] --> B{有文档?}
  B -- 否 --> C[空状态: 新建/打开]
  B -- 是 --> D[编辑 ｜ 预览]
  C --> D
```

## 数学与分隔线

普通段落到此为止。

***

最后一行：试试切换主题（工具栏右上的日月图标），看编辑器与预览是否一起变色。
